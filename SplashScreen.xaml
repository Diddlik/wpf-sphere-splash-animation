<Window x:Class="WpfSphereSplash.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading..." Height="600" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        Loaded="Window_Loaded">
    
    <Grid>
        <!-- Dunkler Hintergrund -->
        <Rectangle Fill="#FF1A1A1A" RadiusX="10" RadiusY="10"/>
        
        <!-- Canvas für die Animation -->
        <Canvas x:Name="AnimationCanvas" 
                Width="600" Height="600"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
            <!-- Kreise werden hier dynamisch hinzugefügt -->
        </Canvas>
        
        <!-- Optional: Loading Text -->
        <TextBlock x:Name="LoadingText"
                   Text="Laden..."
                   FontSize="20"
                   Foreground="#FF9E9E9E"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,50"
                   Opacity="0">
            <TextBlock.RenderTransform>
                <TranslateTransform Y="20"/>
            </TextBlock.RenderTransform>
        </TextBlock>
    </Grid>
</Window>